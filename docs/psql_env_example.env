# Example environment variables for PostgreSQL connection
# Copy this to .env (in the repo root) and fill values. Do NOT commit secrets.

PGHOST=db.example.com
PGPORT=5432
PGUSER=alice
PGPASSWORD=replace_with_secret
PGDATABASE=retail_db
